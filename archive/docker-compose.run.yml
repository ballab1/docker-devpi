
services:

  devpi:
      image: ${DOCKER_REGISTRY:-}docker.io/muccg/devpi:4.5.1
    volumes:
      - ./data:/data
    ports:
      - 3141:3141
    environment:
      - DEVPI_PASSWORD=
    restart: always