
services:

  devpi:
      image: s2.ubuntu.home:5000/docker.io/muccg/devpi:4.5.1
    volumes:
      - ./data:/data
    ports:
      - 3141:3141
    environment:
      - DEVPI_PASSWORD=
    restart: always